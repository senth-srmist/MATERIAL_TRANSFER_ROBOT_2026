<?xml version="1.0"?>
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <ReactiveSequence name="NavigateWithTileCheck">
          <TileCheckAction name="tile_check" config_file=""/>
          <PipelineSequence name="PlanAndFollow">
            <RateController hz="1.0">
              <Sequence>
                <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
                <SmoothPath path="{path}" smoother_id="simple_smoother"/>
              </Sequence>
            </RateController>
            <ReactiveSequence name="HumanAwareFollow">
              <Inverter>
                <Sequence name="HandleHumanBlocking">
                  <!-- Condition: is a human blocking? SUCCESS = yes -->
                  <HumanBlockingPath path="{path}"/>
                  <!-- Wait here (RUNNING) until human clears (SUCCESS), calls /speak service -->
                  <WaitUntilHumanClears path="{path}"/>
                </Sequence>
              </Inverter>
              <!-- Only reached when no human is blocking -->
              <FollowPath path="{path}" controller_id="FollowPath"/>
            </ReactiveSequence>
          </PipelineSequence>
        </ReactiveSequence>
      </PipelineSequence>
      <!-- RECOVERY -->
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <ClearEntireCostmap service_name="global_costmap/clear_entirely_global_costmap"/>
          <ClearEntireCostmap service_name="local_costmap/clear_entirely_local_costmap"/>
          <Spin spin_dist="1.57"/>
          <Wait wait_duration="5"/>
          <BackUp backup_dist="0.30" backup_speed="0.05"/>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
